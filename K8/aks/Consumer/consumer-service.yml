apiVersion: v1
kind: Service
metadata:
  name: event-consumer-service
  #namespace: qa
  labels:
    version: 1.0.0
    product: speech-event-consumer
    environment: qa
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: speech-event-consumer
    env: qa